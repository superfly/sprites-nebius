# Requirement-to-evidence checklist

Updated 16 September 2026. Implementation is not live acceptance. Keep private
identifiers and original production/billing evidence outside a public checkout.

| Requirement | Current evidence / remaining gate |
| --- | --- |
| S1 | Historical live connector injection check passed; retain original dated evidence |
| S2 | Historical invalid-caller-key override check passed; retain original dated evidence |
| S3 | Live 2026-09-16: Chat Completions and Responses each completed with 50 text deltas spread over 2.00s and 1.66s respectively, using Qwen3-30B-A3B-Instruct-2507 |
| S4 | GET denials observed; full intended policy and endpoint coverage still pending approval |
| S5 | Gateway emits attributed records in deployed code; actual records for this batch still need authorized inspection |
| S6 | MIT-pinned adapter, safe transport, cancellation/conversion tests; real Claude service/tool round trip pending |
| B1 | License, pinned runtime, offline CI, template tests and secret scans implemented; GitHub CI has not run for this local-only repository |
| B2 | Four-agent configuration/templates, dry-run, discovery, backups, recovery and scoped restore implemented; native live certification pending |
| B3 | Loopback adapter and ownership-checked Sprite service lifecycle implemented; live service startup/restart pending |
| B4 | Gateway probes, permission-gated POST denial, evidence ledger/reconciliation and host capture scanner implemented; full cross-context collection/orchestration remains incomplete |
| B5 | Conditional quickstart, exact candidate installation pins and connector guide written; independent unaided-admin acceptance still required |
| B6 | Exact, attributable reconciliation tooling/guide implemented; live V10 result pending |
| L1 | Offline self-review and point-in-time dependency audit performed; independent security sign-off still required |
| L2 | Not published; requires explicit authorization |
| L3 | No outreach; requires explicit authorization and reviewed usage evidence |
| V1 | Host-only matcher and coverage checks tested with fake captures; actual before/after filesystem and all-process-environment collection not performed |
| V2 | Historical laptop 401 recorded; no fresh rerun this implementation batch |
| V3 | Historical unlabeled-Sprite 403 recorded; no fresh rerun this implementation batch |
| V4 | CLI equivalent only; Gateway Playground route still untested |
| V5–V7 | Configuration and candidate versions prepared; real exact-OK native-agent checks pending approval |
| V8 | Real Claude edit/test/streamed completion pending a separately scoped code-execution allowance |
| V9 | One-POST denial probe implemented; not executed, requires separate authorization and dispatch evidence |
| V10 | This batch reported 46 input / 102 output tokens, but client response usage is not gateway accounting; billing snapshot predates the batch and cannot establish reconciliation |

The 16 September live batch used exactly two authorized inference requests,
each capped at 256 requested output tokens, with no retries. That allowance is
exhausted. No agent run, label/policy change, service creation, POST denial,
publication, ticket update or outreach is implied by those results.

Approval flags in these tools are deliberate friction, not permission grants.
Obtain fresh authority for the exact target, execution and spending scope.
